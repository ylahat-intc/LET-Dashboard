# ── SECRETS — NEVER COMMIT ───────────────────────────────
.msal_token_cache.bin
azure_config.txt

# ── LARGE CACHE FILES (rebuilt daily, not needed in git) ─
let_data_cache.json
let_data_cache_excel.json
refresh_log.txt

# ── GENERATED / INTERMEDIATE FILES ───────────────────────
LET_Dashboard.aspx
site_region_map.json
*.xlsx
*.xlsm
push_source.gitignore

# ── IMAGE DUPLICATES (favicon.png in repo is the correct one) ─
Designer (4).png
Designer (5).png

# ── DEBUG / TEMP SCRIPTS ─────────────────────────────────
_debug_*.py
_cost_audit.py
check_fields.py
replace_logo.py

# ── EDITOR ───────────────────────────────────────────────
.vscode/
*.pyc
__pycache__/
